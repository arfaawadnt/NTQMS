export const environment = {
  production: true,
  // Same-origin by default: the SPA is served behind the same reverse proxy as
  // the API. Override at deploy time by editing this file before building, or
  // point at an absolute API base for local development.
  apiBaseUrl: '/api',
};
